{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "hook-stdin-posttooluse",
  "title": "PostToolUse Hook Stdin",
  "description": "JSON structure received via stdin by PostToolUse hooks from Claude Code",
  "type": "object",
  "properties": {
    "tool_name": {
      "type": "string",
      "description": "Name of the tool that was called"
    },
    "tool_input": {
      "type": "object",
      "description": "Input parameters that were passed to the tool",
      "additionalProperties": true
    },
    "tool_result": {
      "description": "Result returned by the tool (string or object)"
    }
  },
  "required": ["tool_name"],
  "additionalProperties": true
}
