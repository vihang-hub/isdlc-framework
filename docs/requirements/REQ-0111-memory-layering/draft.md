# REQ-0111 — Memory layering — user/project/session

Preserve 3-layer memory model: user profile, project roundtable memory, session records. Preserve: merge behavior, formatted context injection, writeSessionRecord(), compaction, dual-path search (vector + fallback), weighting, recency, decay.
