# REQ-0108 — Analyze lifecycle implementation

Implement chosen lifecycle architecture (ADR-CODEX-005: separate subsystem). Cover: analyze entry routing (parse flags, prefetch, hydrate context, read meta.json, staleness, sizing), bug classification gate, bug-gather routing, roundtable routing. Preserve 6-way parallel prefetch.
