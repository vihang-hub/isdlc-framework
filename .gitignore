# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/

# Dependencies
node_modules/

# =============================================================================
# RUNTIME DIRECTORIES (created when framework is installed into projects)
# =============================================================================
# These directories are generated by `isdlc init` — each developer recreates
# them locally. They contain symlinks, runtime state, and user-specific config.
.claude/
.isdlc/

# Hook activity log (generated at runtime by logHookEvent)
hook-activity.log

# =============================================================================
# EPHEMERAL / PERSONAL FILES
# =============================================================================
# Session-specific notes (not useful across developers)
NEXT-SESSION.md

# Development session logs
docs/SESSION-*.md

# Agent backups from development
src/claude/agents-backup/

# Archive of old documentation
docs/archive/

# Local setup scripts (contains personal remote URLs)
setup-remotes.sh

# =============================================================================
# DOGFOODING SCRIPTS (one-off internal tooling)
# =============================================================================
framework-info.md
scripts/migrate-docs.sh
src/isdlc/scripts/generate-report.sh
src/isdlc/scripts/validate-state.sh
src/isdlc/checklists/update-agents.txt
